name: nutri_app
description: "Aplicativo nutricional desenvolvido em Flutter e Firebase."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Ícones padrão iOS/Material
  cupertino_icons: ^1.0.8

  # 🔹 Firebase
  firebase_core: ^3.7.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
